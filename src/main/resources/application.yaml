spring:
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true
  datasource:
    hikari:
      jdbc-url: jdbc:h2:mem:testdb
      driver-class-name: org.h2.Driver
      username: sa
      password:
  session:
    store-type: jdbc
  profiles:
    include: oauth
